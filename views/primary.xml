<?xml version="1.0" encoding="utf-8"?>
<odoo>

    
    
        <record id="action_school_primary" model="ir.actions.act_window">
            <field name="name"> primary </field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">school.student</field>
            <field name="view_mode">tree,kanban,form</field>
            <field name="context">{}</field>
            <field name="domain">[('age', '&lt;=', 12)]</field>
              
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new  primary ?
                </p>
            </field>
        </record>
    
    
     <menuitem id="menu_primary"
            name="primary"
            parent="school_student_root"
            action="action_school_primary"
            sequence="20"/>
    
    
</odoo>